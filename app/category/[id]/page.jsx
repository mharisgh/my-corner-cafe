import React from 'react'

function getCategory(id){
  const res = fetch()
}

const categoryList = ({params}) => {
  return (
    <div>Items list here {params.categoryId}</div>
  )
}

export default categoryList